using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spacetostart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //  public enum asleep RigidbodySleepMode2D.StartAsleep
        //public enum awake RigidbodySleepMode2D.StartAwake;
    }

    // Update is called once per frame
    void Update()
    {
        //  if (asleep == RigidbodySleepMode2D.StartAsleep)
        //  {

        //     if (Input.GetKeyDown("space"))
        //     {

        //        asleep = RigidbodySleepMode2D.StartAwake;


        //  }
        //  }
    }
}
